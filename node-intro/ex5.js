// module system in node
// every file in node is a separate room
// const getFavColor = require("./lib.js");

// console.log(getFavColor());

const dtLib = require("./lib");

console.log(dtLib.getFavColor());
console.log(dtLib.getMehColor());
